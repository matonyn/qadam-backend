#
# Render Free plan friendly requirements.
# Intentionally excludes very heavy deps (torch/transformers/datasets/prophet).
# The codebase has graceful fallbacks when these are missing.
#
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1

# Core utils
python-dotenv==1.0.1
httpx==0.27.0
loguru==0.7.2

# Recommendation + crowd (moderate)
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.0
implicit==0.7.2
lightgbm==4.3.0

# Optional scraping/analytics pieces removed for memory
